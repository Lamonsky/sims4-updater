<Window x:Class="sims4_updater.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:sims4_updater"
        xmlns:helpers="clr-namespace:sims4_updater.Helpers"
        mc:Ignorable="d"
        Title="MainWindow" Height="900" Width="1600">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45" />
            <RowDefinition Height="45" />
            <RowDefinition Height="*" />
            <RowDefinition Height="45" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="150" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Select folder with Sims 4:" />
            <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Sims4Model.GamePath}" />
            <Button Grid.Column="2" Grid.Row="0" Content="Browse" Command="{Binding BrowseCommand}" />
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="150" />
            </Grid.ColumnDefinitions>
            <ProgressBar Grid.Column="0" Minimum="0" Maximum="100" Value="{Binding Source={x:Static helpers:StaticsVariables.Instance}, Path=Progress}" />
            <Button Grid.Column="1" Grid.Row="2" Content="Scan" Command="{Binding ScanCommand}" />
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" >
                <ListBox ItemsSource="{Binding Sims4Model.AllDlcs.Sims4DLCs}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding ToInstall}" Margin="5"/>
                                <TextBlock Text="{Binding Code}" Margin="5" Width="50" />
                                <TextBlock Text="{Binding Name}" Margin="5" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding Logger.Logs}" TextWrapping="Wrap" />
            </ScrollViewer>
        </Grid>
        <Button Grid.Row="3" Content="Install" Command="{Binding InstallCommand}"/>
    </Grid>
</Window>
